# This file exists for compatibility. The canonical dependency source is pyproject.toml.
# Install with: pip install -e .
# Install dev deps: pip install -e ".[dev]"
# Install tracking: pip install -e ".[tracking]"
-e .
